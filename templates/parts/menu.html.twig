<div class="container-fluid">
	<div id="bg-app" class="position-absolute"></div>
	<div class="row flex-nowrap">
		<div class="shadow-lg col-3 col-md-2 col-xl-2 px-sm-2 px-0 bg-white">
			<div class="d-flex flex-column align-items-center px-2 pt-2 text-white min-vh-100 text-center">
				<a class="navbar-brand mx-auto" href="{{path('app_home')}}">
					<div>
						<img class="s-1 d-none d-lg-inline w-100" src="{{asset('build/images/pixel-online.gif')}}" alt="Pixel Online Creation">
					</div>
				</a>
				<button class="navbar-toggler d-lg-none" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavId" aria-controls="collapsibleNavId" aria-expanded="false" aria-label="Toggle navigation">
					<span class="badge bg-primary">Pixel</span>
				</button>
				<ul class="nav nav-navbar flex-column mb-sm-auto mb-0 align-items-center" id="menu">
					{% if is_granted('ROLE_ADMIN') %}
						<li class="nav-item my-2 col-12">
							<a class=" text-primary fw-bold nav-link d-flex flex-nowrap" href="{{path('app_admin_home')}}" title="Admin Interface">
								<div class="col-3 d-flex align-items-center">
									<i class="fs-2 bi-speedometer2"></i>
								</div>
								<div class="col-9 d-flex align-items-center justify-content-center">
									<span class="s-1 d-none d-sm-inline">Interface Administrateur</span>
								</div>
							</a>
						</li>
					{% endif %}
					{% for menu in listMenu %}
						{% if menu.text == 'Deconnexion' %}
							<li class="nav-item my-2 col-12">
								<a class=" text-danger fw-bold nav-link d-flex flex-nowrap {% if routeName == menu.url %} actived{% endif %}" href="{{menu.url}}" title="{{menu.title}}">
									<div class="col-3 d-flex align-items-center">
										<i class="fs-2 {{menu.icon}}"></i>
									</div>
									<div class="col-9 d-flex align-items-center justify-content-center">
										<span class="s-1 d-none d-sm-inline">{{menu.text}}</span>
									</div>
								</a>
							</li>
						{% else %}
							<li class="nav-item my-2 col-12">
								<a class=" text-dark fw-bold nav-link d-flex flex-nowrap {% if routeName == menu.url %} actived{% endif %}" href="{{menu.url}}" title="{{menu.title}}">
									<div class="col-3 d-flex align-items-center">
										<i class="fs-2 {{menu.icon}}"></i>
									</div>
									<div class="col-9 d-flex align-items-center justify-content-center">
										<span class="s-1 d-none d-sm-inline">{{menu.text}}</span>
									</div>
								</a>
							</li>
						{% endif %}
					{% endfor %}
				</ul>
				<hr>
			</div>
		</div>
		<div class="col-9 col-md-10 py-3">
